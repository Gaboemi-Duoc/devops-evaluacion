# DevOps Evaluacion 2

Guia AWS:

Crear vpc
Crear 3 instancias de AWS, asegurarse de asignar rol IAM y ejecutar el sgte comando:

>#!/bin/bash
>sudo yum update -y
>sudo yum install -y docker
>sudo systemctl start docker
>sudo systemctl enable docker
>sudo usermod -a -G docker ec2-user

